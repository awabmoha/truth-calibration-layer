# Notebooks

This folder contains notebook artifacts for running TCL-v0 validation checks outside the local CPU-only machine.

## Current Recommendation

Use Kaggle first for the free-GPU dry run, with Colab Free as fallback.

Notebook:

- `tcl_v0_kaggle_free_gpu_dry_run.ipynb`

The notebook is not a completed validation result. It is an execution helper for checking whether free GPU can support the extended-validation path.
